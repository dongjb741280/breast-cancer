# LLM 编排 + 结构化输出
langgraph>=0.2.60
langchain-core>=0.3.0
langchain-anthropic>=0.2.0

# 指南 RAG（LlamaIndex）
llama-index-core>=0.11.0
llama-index-embeddings-openai>=0.2.0   # 向量检索用；不配 OPENAI_API_KEY 则自动退回 BM25（关键词，无需 embedding）

# 配置与类型
pydantic>=2.7.0
python-dotenv>=1.0.0
