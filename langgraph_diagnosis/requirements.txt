# LLM 编排 + 结构化输出
langgraph>=0.2.60
langchain-core>=0.3.0
langchain-anthropic>=0.2.0

# 指南 RAG（LlamaIndex）
llama-index-core>=0.11.0
llama-index-embeddings-huggingface>=0.4.0  # 本地中文向量（BAAI/bge-small-zh-v1.5；拉取 sentence-transformers+torch）
llama-index-retrievers-bm25>=0.3.0         # 未装 huggingface 依赖时退回 BM25（关键词，无需 embedding）
modelscope>=1.20.0                          # 国内下载 embedding 模型（阿里魔搭，替代 HuggingFace 直连）

# 配置与类型
pydantic>=2.7.0
python-dotenv>=1.0.0
